## Requirements
- Redis
- Node

### Install

````bash
(cd client && npm install)
(cd server && npm install)
````
### Run

#### Server
````bash
cd server && DEBUG=koa* node index.js
````

#### Client
````bash
cd client && npm start
````

